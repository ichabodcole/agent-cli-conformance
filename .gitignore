node_modules/
dist/
# `bun run docs:build` — the rendered wiki. Already covered by `dist/` above; named
# explicitly so the output location is discoverable from here.
docs/dist/
.DS_Store
*.log
.superpowers/
.operator
# Working material an inquiry produced that has not earned a durable home. Untracked on
# purpose: nothing can depend on it, so deleting it — or never touching it again — costs
# nothing and owes no one a decision. See docs/research/README.md.
.scratch/
