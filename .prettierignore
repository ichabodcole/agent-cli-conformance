# Generated by release-please on every release, in its own style — `*` bullets where prettier
# wants `-`. Formatting it would be a fight with the generator: the next release rewrites the
# file and the reformat is lost, so the gate would fail again on the following release PR.
#
# Same rule as `package.json`'s version field and `.release-please-manifest.json`: release-please
# owns this file, and a hand-authoring style gate has no business asserting over it.
CHANGELOG.md

# Rendered wiki output — gitignored, but `prettier --check '**/*.md'` would still walk it locally.
docs/dist/
